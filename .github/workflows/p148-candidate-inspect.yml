name: P148 Candidate Inspect

on:
  push:
    branches:
      - p148-machine-reconciliation

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify canonical inventory
        run: python scripts/reconstruct_candidate_inventory.py
      - name: Export p148 candidates
        run: python scripts/export_candidate_page.py --page 148
      - name: Inspect p149 opening
        run: |
          python scripts/export_candidate_page.py --page 149 | python -c 'import sys,json; rows=[json.loads(x) for x in sys.stdin if x.strip()]; [print(json.dumps(r,ensure_ascii=False,sort_keys=True)) for r in rows if r.get("candidateOrderInColumn",99)<=6]'
